import { StyleSheet } from 'react-native';
var styles = StyleSheet.create({
    container: {
        flexDirection: 'row',
        flexWrap: 'wrap',
        margin: -5,
    },
    item: {
        width: '50%',
        padding: 5,
    },
});
export default styles;
